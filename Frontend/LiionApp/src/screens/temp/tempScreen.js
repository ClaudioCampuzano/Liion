import React from 'react';
import { Text, View } from 'react-native';

const tempScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
      }}>
      <Text>Esta es una pantalla temporal, en la siguiente version debemos integrar con al viaje en prograso y redirijir hacia alla</Text>
    </View>
  )
}
export default tempScreen;